apiVersion: v1
kind: Service
metadata:
  name: spec-dep
  namespace:
spec:
  type: LoadBalancer
  selector:
    web: webExpre
  ports:
    - name: port-define
      port: 80
      protocol: TCP
      targetPort: 80
