apiVersion: apps/v1
kind : Deployment
metadata:
  name: slee-dep
  namespace: prod
  annotations:
    kubernetes.io/change-cause: "slee-image"
spec:
  minReadySeconds: 5
  replicas: 3
  selector:
    matchExpressions: 
      - key: app
        operator: In
        values: 
          - slee
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
  template:
    metadata:
      name: dep-pod
      labels:
        app: slee
    spec:
      containers:
        - name: dep-cont
          image: srinuparella/cssi:slee
          ports:
            - containerPort: 80
              # hostport: 80
              protocol: TCP
          startupProbe:
            httpGet:
              path: /
              port: 80
          livenessProbe:
            httpGet:
              path: /
              port: 80
          readinessProbe:
            httpGet:
              path: /
              port: 80
          resources:
            requests:
              cpu: "200m"
              memory: "350Mi"
            limits:
              cpu: "500m" 
              memory: "1000Mi"

# ---
# apiVersion: apps/v1
# kind: Deployment
# metadata:
#   name: slee-dep
#   namespace: prod
#   annotations:
#     kubernetes.io/change-cause: "slee-image"
# spec:
#   minReadySeconds: 5
#   replicas: 3
#   selector:
#     matchLabels:        # changed from matchExpressions for simplicity
#       web: webexpre
#   strategy:
#     type: RollingUpdate
#     rollingUpdate:
#       maxSurge: 1
#       maxUnavailable: 1
#   template:
#     metadata:
#       labels:
#         web: webexpre
#     spec:
#       containers:
#         - name: dep-cont
#           image: srinuparella/cssi:slee
#           ports:                     # uncommented ports so Service can target pods
#             - containerPort: 80
#               protocol: TCP
#           startupProbe:
#             httpGet:
#               path: /
#               port: 80
#             failureThreshold: 3
#             periodSeconds: 5
#           livenessProbe:
#             httpGet:
#               path: /
#               port: 80
#             initialDelaySeconds: 10
#             periodSeconds: 10
#           readinessProbe:
#             httpGet:
#               path: /
#               port: 80
#             initialDelaySeconds: 5
#             periodSeconds: 5
#           resources:
#             requests:
#               cpu: "200m"
#               memory: "350Mi"
#             limits:
#               cpu: "500m"
#               memory: "1000Mi"


